# Enter one VM parameter per line
# To include another file, uncomment the following line:
# -include-options [path to other .vmoption file]
-server
-Xmx18G

# Set the logging level (INFO includes messages from progress monitoring, FINE includes more detailed messages)  
-Dsnap.log.level=FINE

# Put out (or do not put out) information for debugging (this is not related to logging)
-Dsnap.debug=false
